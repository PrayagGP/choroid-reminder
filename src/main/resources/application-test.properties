# Test Configuration - Use fake data instead of real API calls
test.use-fake-data=true

# Application Configuration
spring.application.name=Choroid-Reminder-Service-TEST
server.port=8083

# Disable database for testing (no DB required with fake data)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Email Configuration for Testing
# IMPORTANT: Replace these with your actual Gmail credentials for testing!
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=prayagwarp2@gmail.com
spring.mail.password=nbll updw xpes zjsp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Test Reminder Configuration
reminder.from-email=test-noreply@choroid.com
reminder.from-name=Choroid Test System

# Fast scheduling for testing (check every 30 seconds instead of 5 minutes)
reminder.scheduler.enabled=true
reminder.scheduler.session-check-interval=30000
reminder.scheduler.cleanup-interval=300000

# Test reminder timing (reduced for faster testing)
reminder.before-session.first=30
reminder.after-session.feedback=30

# Logging Configuration (more verbose for testing)
logging.level.com.ddbs.choroid_reminder_service=INFO
logging.level.com.ddbs.choroid_reminder_service.test=INFO
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.scheduling=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Actuator for testing
management.endpoints.web.exposure.include=health,info